# IntelliConf 国际学术会议会务系统 · 使用与演示文档

> 版本：v1.0 ｜ 更新日期：2026-09-06  
> 适用对象：会议组织者 / 演示评审人员  
> 文档目标：从创建会议到各流程操作的全流程使用说明，以及三大核心模块的演示脚本与技术实现说明

---

## 目录

- [第一部分　系统使用文档](#第一部分系统使用文档)
  - [一、系统概述与角色](#一系统概述与角色)
  - [二、快速开始：创建第一场会议](#二快速开始创建第一场会议)
  - [三、核心模块使用说明](#三核心模块使用说明)
  - [四、其他辅助功能](#四其他辅助功能)
- [第二部分　核心模块演示文档](#第二部分核心模块演示文档)
  - [演示一：全流程管理（以时间轴为核心）](#演示一全流程管理以时间轴为核心)
  - [演示二：邮件闭环系统（绑定邮箱 → 审核 → 自动发送）](#演示二邮件闭环系统绑定邮箱--审核--自动发送)
  - [演示三：潜在投稿人发现（热点主题 → 学者检索）](#演示三潜在投稿人发现热点主题--学者检索)
- [第三部分　技术实现与稳定性说明](#第三部分技术实现与稳定性说明)

---

# 第一部分　系统使用文档

## 一、系统概述与角色

这是一套面向国际学术会议**全过程**的会务辅助系统，覆盖「筹备 → 征稿 → 评审 → 注册 → 会议期间 → 会后归档」全生命周期。系统基于关键日期自动生成阶段流程与任务计划，并提供邮件闭环自动化与潜在投稿人智能发现能力。

### 1.1 三大核心模块

| 模块          | 定位         | 核心能力                                                   |
| ----------- | ---------- | ------------------------------------------------------ |
| **全流程管理**   | 会议骨架与执行主线  | 按关键日期自动生成六阶段时间轴，每阶段展开为多个可指派、可执行、可追溯的事务任务               |
| **邮件闭环系统**  | 节点触发的自动化通知 | 绑定会议发件邮箱；流程节点临近时自动生成邮件草稿；负责人审核（可改正文、勾选收件人）后到点自动发送      |
| **潜在投稿人发现** | 会前征稿的精准触达  | 基于热点主题关键词，从多个学术数据源检索相关学者，从公开论文/PDF 提取邮箱，评分入库后供组织者审核发邀请 |

### 1.2 用户角色

| 角色                               | 权限说明                             |
| -------------------------------- | -------------------------------- |
| **组织者（Organizer）**               | 创建会议、生成流程、指派任务、审核邮件、发起投稿人发现、管理成员 |
| **任务负责人（Principal）**             | 执行被指派的任务、上传附件、提交完成、审核该任务关联的邮件    |
| **审稿人（Reviewer）/ 投稿作者（Author）等** | 作为邮件收件对象，接收系统自动发出的通知             |

---

## 二、快速开始：创建第一场会议

### 第 1 步：创建会议

进入「我的会议」门户页，新建会议，填写基础信息：

| 字段                                       | 说明        | 示例                      |
| ---------------------------------------- | --------- | ----------------------- |
| 会议标题（title）                              | 全称        | 2026 国际人工智能与教育大会        |
| 会议简称（shortName）                          | 唯一标识，用于短链 | ICAIE2026               |
| 会议网站（websiteUrl）                         | 官方网站地址    | <https://icaie2026.org> |
| 联系邮箱（contactEmail）                       | 组委会公共邮箱   | <icaie2026@xxx.com>     |
| 主办方（host）/ 协办方（coOrganizer）              | 主协办单位     | —                       |
| 会议起止时间（startTime / endTime）              | 会议实际召开时间  | 2026-12-10 ~ 2026-12-13 |
| 举办地（country / province / city / address） | 会议地点      | 中国 · 杭州                 |

> 后端接口：`POST /api/intelli-conf/v1/conference`

### 第 2 步：设置重要日期

会议创建后进入「会议信息」页，补全关键日期。这些日期是系统**自动生成阶段流程与任务计划的时间基准**：

| 重要日期                                  | 用途              |
| ------------------------------------- | --------------- |
| 投稿截止（paperSubmissionDeadline）         | 征稿阶段结束 / 评审阶段开始 |
| 录用通知（notificationOfAcceptance）        | 评审阶段结束 / 注册阶段开始 |
| 终稿提交（cameraReadySubmission）           | 注册阶段关键节点        |
| 早鸟注册截止（earlyBirdRegistration）         | 注册缴费阶段节点        |
| 会议起止日期（conferenceStartDate / EndDate） | 会议期间阶段          |

> 后端接口：`PUT /api/intelli-conf/v1/conference/{shortName}/important-dates`

### 第 3 步：生成阶段流程（时间轴）

在「流程设置」向导中：

1. 点击**预览阶段**，系统会根据重要日期自动推算六个标准阶段及其计划起止时间；
2. 确认无误后点击**生成阶段**，时间轴正式建立。

系统标准六阶段（按时间顺序）：

| 顺序 | 阶段代码                   | 阶段名称 | 时间依据            |
| -- | ---------------------- | ---- | --------------- |
| 1  | CALL_FOR_PAPERS        | 征稿   | 会议启动 ~ 投稿截止     |
| 2  | REVIEW                 | 评审   | 投稿截止 ~ 录用通知     |
| 3  | REGISTRATION           | 注册缴费 | 录用通知 ~ 早鸟截止     |
| 4  | CONFERENCE_PREPARATION | 会议筹备 | 录用通知 ~ 会议开始前    |
| 5  | CONFERENCE_DAYS        | 会议期间 | 会议开始 ~ 会议结束     |
| 6  | POST_CONFERENCE        | 会后归档 | 会议结束 ~ 结束后 60 天 |

> 后端接口：`GET /api/intelli-conf/v1/conference/{key}/stages/preview`（预览）、`POST .../stages/generate`（确认生成）

### 第 4 步：生成任务计划

阶段生成后，系统会按阶段模板展开出**可执行的事务任务**。在向导中点击**预览任务 → 生成任务**。每个阶段会展开为多个任务，例如：

| 阶段   | 典型任务                                                 |
| ---- | ---------------------------------------------------- |
| 征稿   | Call for Papers 通知、潜在投稿人邀请（邮件类）                      |
| 评审   | 分配审稿人（ASSIGN_REVIEWERS）、发送评审提醒（SEND_REVIEW_REMINDER） |
| 注册   | 注册缴费提醒（REMIND_REGISTRATION）                          |
| 会议期间 | 现场会务执行（RUN_CONFERENCE）                               |
| 会后   | 会后归档（POST_CONFERENCE_WRAPUP）                         |

> 后端接口：`GET /api/conferences/{id}/tasks/preview`、`POST /api/conferences/{id}/tasks/generate`

### 第 5 步：进入工作台

生成完成后进入「会议工作台」（Dashboard）。工作台顶部展示：

- **当前阶段**与全流程完成度仪表盘；
- 事务快捷卡片：当前待办、逾期事务、**待审核（邮件）**、待指派；
- 全流程时间轴；
- 事务列表（支持按状态筛选、按阶段筛选）。

---

## 三、核心模块使用说明

### 3.1 全流程管理

#### 时间轴视图

工作台中部展示全流程时间轴，每个阶段为一个里程碑节点，显示计划起止时间与完成进度。可对节点执行：

- **启动节点**：阶段开始执行；
- **完成节点**：阶段结束，推进到下一阶段；
- **跳过节点**：特殊情况下跳过该阶段（需填写说明）。

> 后端接口：`POST /api/conferences/{id}/timeline/{nodeCode}/start | complete | skip`

#### 事务任务管理

每个阶段展开为多个任务。每个任务支持完整的事务闭环操作：

| 操作    | 说明              | 接口                                |
| ----- | --------------- | --------------------------------- |
| 指派负责人 | 将任务分派给具体成员      | `POST .../tasks/{id}/assign`      |
| 开始执行  | 标记任务进入执行中       | `POST .../tasks/{id}/start`       |
| 上传附件  | 上传成果文件（如征稿通知文档） | `POST .../tasks/{id}/attachments` |
| 完成提交  | 提交完成说明，系统校验     | `POST .../tasks/{id}/complete`    |
| 转交    | 转给其他负责人         | `POST .../tasks/{id}/transfer`    |
| 驳回    | 上级驳回需返工         | `POST .../tasks/{id}/reject`      |
| 取消    | 取消任务            | `POST .../tasks/{id}/cancel`      |
| 评论    | 任务内沟通           | `POST .../tasks/{id}/comments`    |
| 操作日志  | 查看完整流转记录        | `GET .../tasks/{id}/logs`         |

任务有系统自检能力（`system-check`），例如邮件类任务会校验发件邮箱是否已绑定、收件人是否就绪。

#### 自动调度

后台调度器 `MilestoneSentinel` 周期巡检，到点的里程碑自动激活；`TaskDispatcher` 在里程碑激活时自动派发该阶段关联的任务实例——无需人工逐个创建。


### 3.2 邮件闭环系统

邮件闭环解决「该发的通知总能按时、按对象、经审核后自动发出」的问题。

#### 第一步：绑定会议发件邮箱

在「会议邮箱」页绑定一个 SMTP 发件账户。系统支持多种服务商预设：

| 服务商         | 主机                 | 端口             | 密码类型             |
| ----------- | ------------------ | -------------- | ---------------- |
| 腾讯企业邮       | smtp.exmail.qq.com | 465 (SSL)      | 邮箱登录密码 / 客户端专用密码 |
| QQ 个人邮      | smtp.qq.com        | 465 (SSL)      | 授权码              |
| 网易 163      | smtp.163.com       | 465 (SSL)      | 授权码              |
| Gmail       | smtp.gmail.com     | 465 (SSL)      | 应用专用密码           |
| Outlook/365 | smtp.office365.com | 587 (STARTTLS) | 账号密码 / 应用专用密码    |
| 自定义 SMTP    | 手填                 | 手填             | —                |

绑定流程：选择服务商 → 填发件邮箱 → 填授权码/密码 → 系统实时校验 SMTP 连接 → 通过则 AES 加密存储密码。

> 关键稳定性点：密码提交前后端**三重清洗空白字符**（绑定清洗 + 发送兜底 + 前端提交前清洗），并按服务商给出细粒度认证失败提示，避免复制授权码带空格导致认证失败。

#### 第二步：邮件草稿自动生成

后台调度器 `generateUpcomingPlans` 会提前 7 天为临近的邮件类任务（taskType=EMAIL）自动生成邮件计划（`conf_task_mail_plan`），状态为 `PENDING_REVIEW`（待审核）。邮件主题与正文按会议场景模板渲染（含会议名、关键日期、网站等变量）。

#### 第三步：负责人审核

在「邮件自动化」页或首页「待审核」入口进入审核抽屉：

- **邮件正文以纯文本展示**（自动去除 HTML 标签，便于查看与修改）；
- **收件人列表可勾选**：支持全选 / 清空 / 单独勾选某些人不发；
- 可修改主题、正文、计划发送时间；
- 操作：**审核通过并加入发送队列** 或 **拒绝**（填原因）。

> 审核通过后状态变 `APPROVED`，系统记录勾选的收件人邮箱（`selected_emails`），发送时只给勾选的人发。

#### 第四步：到点自动发送

后台调度器 `sendApprovedDuePlans` 周期巡检，把已到计划发送时间且状态为 `APPROVED` 的邮件自动发出：

- 按审核时勾选的收件人过滤（未勾选则发该角色全部）；
- 调用 SMTP 实际发送，逐条记录成功/失败；
- 发送完成状态置 `SENT`（有成功）或 `FAILED`（全失败），失败可**重试**。

#### 邮件状态机

```
PENDING_REVIEW（待审核）
      │
      ├──审核通过──> APPROVED（已审核待发送）──到点──> SENDING ──> SENT（已发送）
      │                                              └──> FAILED（发送异常，可 retry）
      └──拒绝──────> REJECTED（已拒绝，不再发送）
```

### 3.3 潜在投稿人发现

用于会前精准找到研究相关领域的学者，提取其公开邮箱，供组织者审核后发征稿邀请。

#### 创建发现任务

在「人员中心」或「潜在投稿者发现」页发起任务，填写：

| 参数                      | 说明                 | 演示建议值       |
| ----------------------- | ------------------ | ----------- |
| 主题关键词（topicKeywords）    | 多个主题用换行分隔，每个主题独立检索 | 见演示三        |
| 起止年份（yearFrom / yearTo） | 论文发表时间范围           | 2020 ~ 2026 |
| 目标人数（maxAuthors）        | 期望发现的学者数           | 200         |
| 启用 Crossref             | 是否补充检索 Crossref    | 是           |
| 启用邮箱提取                  | 是否从论文页/PDF 提取邮箱    | 是           |

#### 任务执行（异步）

任务提交后后台异步运行，前端轮询进度直到终态。执行链路：

1. **双源论文检索**：OpenAlex + Semantic Scholar 合并去重（按 DOI/标题）；
2. **候选作者聚合**：同名同机构合并为一位候选；
3. **公开邮箱提取**：从论文落地页 / PDF 直链提取邮箱（PDFBox 解析），遵守 robots.txt，主机级熔断，共享 HttpClient keep-alive；
4. **评分入库**：按论文数、机构匹配度、邮箱可信度等评分；
5. **增量续跑**：已入库邮箱不重复抓取，任务可中断、可恢复。

#### 候选审核

发现的学者进入「潜在投稿者发现」页的待审核列表，组织者可：

- 查看学者姓名、机构、论文、邮箱及来源证据；
- **通过**：纳入正式邀请名单；
- **拒绝**：剔除。

审核通过的学者可作为「PROSPECT」角色，被征稿邮件任务作为收件人。

> 关键稳定性点：12 线程并发收割 + 停滞看门狗（10 分钟无进展则主动收尾保留结果，杜绝假死）+ 主机熔断（不可达站点跳过）+ 服务重启时自动恢复遗留的 RUNNING 任务为可续跑状态。

---

## 四、其他辅助功能

| 功能     | 入口         | 说明                    |
| ------ | ---------- | --------------------- |
| 会议信息   | 更多 → 会议信息  | 查看/编辑会议基础信息与重要日期      |
| 临时邮件   | 更多 → 临时邮件  | 手动撰写并即时发送一封邮件（不走审核流程） |
| 委员会初始化 | 设置向导       | 导入主席/审稿人等委员会成员        |
| 人员中心   | 工作台 → 人员中心 | 管理会议成员、角色、发起投稿人发现     |
| 会议邮箱   | 更多 → 会议邮箱  | 绑定/更换发件邮箱账户           |

---

# 第二部分　核心模块演示文档

> 以下三个演示脚本面向评审/答辩场景，按顺序演示可完整展现系统能力。建议演示前重启后端服务，确保最新代码生效。

## 演示一：全流程管理（以时间轴为核心）

### 演示目标

展现系统按关键日期自动生成六阶段时间轴，并逐阶段展开为多个可执行任务的能力。

### 演示前准备

- 已创建一场测试会议（如 ICAIE2026），并填好重要日期；
- 已生成阶段与任务。

### 演示步骤

| 步骤 | 操作                             | 预期效果                                        |
| -- | ------------------------------ | ------------------------------------------- |
| 1  | 进入会议工作台                        | 顶部仪表盘显示当前阶段与全流程完成度；下方展示完整时间轴                |
| 2  | 指向时间轴第一段「征稿」                   | 显示该阶段计划起止时间、进度条                             |
| 3  | 点击「征稿」节点 → 启动                  | 节点状态变为进行中，该阶段下属任务自动出现在事务列表                  |
| 4  | 展开事务列表，按阶段筛选                   | 可见征稿阶段展开的多个任务（如 Call for Papers 通知、潜在投稿人邀请） |
| 5  | 选一个任务 → 指派负责人 → 开始 → 上传附件 → 完成 | 任务状态流转，时间轴进度推进                              |
| 6  | 完成征稿节点                         | 时间轴推进到「评审」阶段，评审任务自动出现                       |
| 7  | 切换不同阶段筛选                       | 展示每阶段任务清单各不相同，体现「阶段→多任务」的展开结构               |

### 演示亮点话术

> 「系统根据我们填写的投稿截止、录用通知等关键日期，自动推算出六个阶段的时间区间，并按阶段模板展开出对应的会务任务。组织者无需手动建任务，只需按时间轴推进，每个阶段的事务自然浮现。」

---

## 演示二：邮件闭环系统（绑定邮箱 → 审核 → 自动发送）

### 演示目标

展现「绑定发件邮箱 → 流程节点自动生成邮件草稿 → 负责人审核（改正文、勾选收件人）→ 到点自动发送」的完整闭环。

### 演示前准备

- 已有会议与成员（至少 2~3 名投稿作者/审稿人角色成员，有真实邮箱）；
- 准备一个可用 SMTP 邮箱（推荐腾讯企业邮或 QQ 邮箱，便于现场验证收到）。

### 演示步骤

| 步骤 | 操作                        | 预期效果                            |
| -- | ------------------------- | ------------------------------- |
| 1  | 工作台 → 更多 → 会议邮箱           | 进入邮箱绑定页                         |
| 2  | 选择「腾讯企业邮」，填发件邮箱与登录密码，点绑定  | 系统校验 SMTP 连接，提示绑定成功；密码 AES 加密存储 |
| 3  | 回到工作台，顶部「待审核」红色徽标显示数字     | 表明有待审核邮件                        |
| 4  | 点「待审核」进入邮件自动化页            | 看到待审核邮件草稿（由流程节点自动生成）            |
| 5  | 打开审核抽屉                    | 正文以**纯文本**展示，无 HTML 标签，可读可改     |
| 6  | 收件人区域：演示「全选」「取消勾选某 1~2 人」 | 标题实时显示「共 N 人，已选 M」              |
| 7  | 修改正文一处文字，点「审核通过并加入发送队列」   | 状态变「已审核待发送」                     |
| 8  | 将计划发送时间改为 1~2 分钟后（或立即触发）  | 等待到点                            |
| 9  | 到点后刷新                     | 状态变「已发送」，显示成功/失败计数              |
| 10 | 登录收件邮箱查收                  | 被勾选的收件人收到邮件，未勾选的没收到             |

### 演示亮点话术

> 「邮件不是组织者手动写的——系统根据流程节点临近自动生成草稿，负责人只需审核。正文可改，收件人可勾选（全选或排除个别人），审核通过后到点自动发出，整个过程留痕、可追溯、可重试。密码加密存储，授权码自动清洗空格，避免认证失败。」

---

## 演示三：潜在投稿人发现（热点主题 → 学者检索）

### 演示目标

展现输入热点研究主题，系统能从公开学术数据源检索到相关学者并提取邮箱，体现「精准找学者」的能力。

### 演示前准备

- 已有会议；
- 建议选 3 个当下热点主题关键词（见下）。

### 推荐热点主题（演示用）

| 领域      | 推荐关键词                                            |
| ------- | ------------------------------------------------ |
| 人工智能与教育 | large language model education / AI in education |
| 计算机视觉   | vision transformer / self-supervised learning    |
| 推荐系统    | graph neural network recommendation              |

### 演示步骤

| 步骤 | 操作                                    | 预期效果                             |
| -- | ------------------------------------- | -------------------------------- |
| 1  | 人员中心 → 潜在投稿者发现 → 发起任务                 | 弹出参数表单                           |
| 2  | 填入 3 个热点主题（每行一个），目标人数 50，年份 2022~2026 | 参数就绪                             |
| 3  | 点击开始                                  | 任务进入后台检索，前端显示进度（已检查论文数 / 已入库候选数） |
| 4  | 等待进度推进（可演示「停止任务」随时中止）                 | 进度持续刷新，不再假死                      |
| 5  | 任务完成（PARTIAL/COMPLETED）               | 候选列表展示发现的学者                      |
| 6  | 查看某位学者详情                              | 显示姓名、机构、论文标题、邮箱、邮箱来源证据（论文页/PDF）  |
| 7  | 选 2~3 位点「通过」，1 位点「拒绝」                 | 通过的进入正式邀请名单                      |
| 8  | 回到邮件闭环，发起一封征稿邮件，收件人勾选刚通过的学者           | 形成「发现学者 → 审核入库 → 精准邀请」闭环         |

### 演示亮点话术

> 「我们输入研究主题，系统从 OpenAlex 和 Semantic Scholar 两个学术数据源检索相关论文，聚合出作者，再从论文页面和 PDF 里提取公开邮箱。多线程并发抓取、不可达站点自动熔断、停滞有看门狗兜底，所以任务稳定不卡死。发现的学者经组织者审核后才正式入库，可被征稿邮件精准触达。」

---

# 第三部分　技术实现与稳定性说明

## 一、整体技术架构

| 层   | 技术栈                                                                      |
| --- | ------------------------------------------------------------------------ |
| 前端  | Vue 3 (`<script setup>`) + TypeScript + Vite + Element Plus + vue-router |
| 后端  | Spring Boot + Java 17 + MyBatis-Plus + Lombok + Hutool + Fastjson2       |
| 数据库 | MySQL（utf8mb4）                                                           |
| 邮件  | JavaMail（`JavaMailSenderImpl`）+ Apache PDFBox（PDF 邮箱提取）                  |
| 调度  | Spring `@Scheduled`（里程碑激活、邮件草稿生成、到点发送、投稿人发现看门狗）                          |
| 安全  | 用户上下文（`UserContext`）+ 角色/成员鉴权 + SMTP 密码 AES 加密存储                         |

## 二、三大模块技术实现

### 2.1 全流程管理

| 关键类/组件                        | 职责                                   |
| ----------------------------- | ------------------------------------ |
| `ConferenceController`        | 会议创建、重要日期、阶段预览/生成                    |
| `ConferenceStageServiceImpl`  | 按重要日期推算六阶段时间区间并落库                    |
| `MilestoneTemplate` 枚举        | 标准里程碑模板（征稿/评审/注册/议程等）                |
| `ConferenceProcessController` | 时间轴查询、节点 start/complete/skip         |
| `ConferenceTaskServiceImpl`   | 任务生成、指派、开始、完成、转交、驳回、取消、评论、附件、日志、系统自检 |
| `TaskDispatcher`              | 里程碑激活时自动派发该阶段任务实例                    |
| `MilestoneSentinel`（调度器）      | 周期巡检，到点自动激活里程碑                       |

**稳定性设计**：

- 时间区间由重要日期自动推算，避免人工填错；
- 里程碑激活与任务派发解耦（事件驱动），单一阶段失败不影响其他阶段；
- 任务有系统自检（`system-check`），执行前校验前置条件（如邮箱是否绑定）。

### 2.2 邮件闭环系统

| 关键类/组件                                       | 职责                                                  |
| -------------------------------------------- | --------------------------------------------------- |
| `ConferenceMailServiceImpl`                  | 邮箱绑定（`bindMailAccount`，密码清洗 + AES 加密 + 连接校验）、直接批量发送 |
| `SmtpMailProvider` / `TencentExmailProvider` | SMTP 发送实现，按服务商细分认证失败提示                              |
| `TaskMailAutomationService`                  | 邮件草稿自动生成、审核、拒绝、重试、到点发送                              |
| `TaskMailAutomationController`               | 待审计数、审核、拒绝、重试接口                                     |
| `TemplateRenderService`                      | 邮件模板变量渲染                                            |
| `conf_task_mail_plan` 表                      | 邮件计划持久化（含 `selected_emails` 勾选收件人）                  |
| `TaskMailPlanSchemaInitializer`              | 表结构幂等初始化（启动自动建表/加列）                                 |

**稳定性设计**：

- **密码三重清洗**：绑定清洗 + 发送兜底 + 前端提交前清洗，杜绝授权码带空格导致 AUTH_FAILED；
- **纯文本正文**：`buildBody` 生成纯文本，`plainBody` 字段对存量 HTML 自动去标签，审核页所见即所得；
- **收件人持久化**：审核勾选的 `selected_emails` 落库，发送时严格按勾选过滤，未勾选不发；
- **状态机闭环**：PENDING_REVIEW → APPROVED → SENDING → SENT/FAILED，REJECTED 独立终态，每态可追溯；
- **幂等**：已发送/发送中的邮件不能重复审核；schema 初始化幂等（启动自动加列不报错）；
- **失败可重试**：FAILED 状态可 `retry` 重新入队。

### 2.3 潜在投稿人发现

| 关键类/组件                                       | 职责                                               |
| -------------------------------------------- | ------------------------------------------------ |
| `AuthorDiscoveryController`                  | 创建任务、启动、取消、查询进度、候选审核                             |
| `AuthorDiscoveryService`                     | 双源检索、并发收割、邮箱提取、评分入库、增量续跑                         |
| `OpenAlexClient` / `SemanticScholarClient`   | 双学术数据源检索（按 DOI/标题去重）                             |
| `PublicEmailExtractor`                       | 共享 HttpClient、主机熔断、robots.txt 校验、PDF（PDFBox）邮箱提取 |
| `AuthorCandidateBuilder`                     | 候选聚合（同名+同机构合并）                                   |
| `CandidateScoringService`                    | 评分（论文数、机构、邮箱可信度）                                 |
| `AuthorIdentityMatcher`                      | 作者身份匹配                                           |
| `DiscoveryJobRunner` / `DiscoveryJobControl` | 异步任务运行、Future 注册、取消中断                            |
| `AuthorDiscoveryProperties`                  | 并发数、时限、熔断阈值等可调参数                                 |

**稳定性设计**：

- **双数据源**：OpenAlex + Semantic Scholar 合并去重，单源故障仍有产出；
- **12 线程并发收割**（`ExecutorCompletionService` 按完成顺序收割），提速且不阻塞；
- **停滞看门狗**：10 分钟无任何候选完成则主动收尾保留已有结果，杜绝任务假死；
- **主机级熔断**：连续失败的主机（如不可达的 arXiv）直接跳过，不反复超时等待；
- **可中止**：`cancel` 接口 + Future 中断，任务运行中可随时停止；
- **可恢复**：服务重启时自动把遗留的 RUNNING 任务恢复为可续跑状态，不留僵尸任务；
- **增量续跑**：预载已入库邮箱，重跑不重复抓取；
- **robots.txt 合规**：抓取前校验，尊重站点爬取策略。

## 三、质量保障

| 维度   | 保障措施                                           |
| ---- | ---------------------------------------------- |
| 单元测试 | 邮件服务、作者发现核心逻辑等 18+ 单测全通过（含授权码清洗、任务失败标记、构造器适配等） |
| 类型安全 | 前端 `vue-tsc` 类型检查 + 后端 Maven 编译均通过             |
| 幂等性  | schema 初始化、任务派发、邮件发送均有幂等保护                     |
| 可观测  | 各模块关键节点均有日志（里程碑激活、邮件发送结果、投稿人发现进度）              |
| 安全   | SMTP 密码 AES 加密存储，接口均有用户鉴权与成员/角色校验              |

---

## 附录：演示前检查清单

- [ ] 后端服务已重启（最新代码生效）
- [ ] 数据库可连接
- [ ] 已创建演示会议并填好重要日期
- [ ] 已生成阶段与任务
- [ ] 已准备一个可用 SMTP 邮箱（腾讯企业邮/QQ）
- [ ] 会议下有 2~3 名真实邮箱的成员（作为收件人）
- [ ] 投稿人发现任务参数已备好（3 个热点主题）
- [ ] 前端已构建最新版本（`npm run build`）

---

*本文档基于 IntelliConf 系统实际代码与接口编写，随系统迭代同步更新。*
