# 潜在投稿者发现功能 — 第二轮诊断与修复报告

日期：2026-09-06 ｜ 范围：`author_discovery` 模块 + 前端用户管理页 ｜ 触发：用户实测"还是不太行"

---

## 一、这次的真正症结（数据实证）

### 1. 任务 8 假死了 2 小时，而不是"在跑"
数据库实测记录（`conf_author_discovery_job`）：

| 任务 | 时间 | 状态 | 论文 | 结果 | 关键信息 |
|---|---|---|---|---|---|
| #7 | 11:25→11:36 | PARTIAL | 1172 | 50/200 | **只检查了 57 位作者就收尾**（疑似旧进程被打断） |
| #8 | 11:38→至今 | RUNNING | 5780 | 58/200 | **进度停在"已检查 20/6000"，11:45→13:45 两小时零推进** |

任务 8 的 16 个在飞抓取线程全部卡死（疑似异常 PDF 解析死循环或大 PDF 并发导致堆/GC 压力），主线程空转等 180 分钟 deadline——**界面看起来像"还在努力跑"，实际已经死了**。这是"不太行"的直接观感来源。

### 2. 本机网络实测（决定产出天花板）
| 数据源 | 直连 | 代理 | 结论 |
|---|---|---|---|
| OpenAlex / Crossref | ✅ ~1.5s | ✅ | 可用，主力源 |
| Semantic Scholar | ❌ 429 | ❌ 429 | 无 API Key 被限流，**第二数据源形同虚设** |
| **arxiv.org** | ❌ 连接重置 | ❌ 连接重置 | **AIED 领域最大 OA PDF 来源整体不可达** |
| eric.ed.gov 主站 | ❌ 超时 | — | robots 校验每次白等 12 秒 |
| files.eric.ed.gov / Springer / PMC / Frontiers | ✅ | ✅ | 可用 |

### 3. 吞吐瓶颈（代码层面）
- 每位候选作者最多 **8 篇论文 × 8 个 URL 顺序抓取**，单请求 12s 超时 → 极端情况单人耗 10+ 分钟；
- **每个 HTTP 请求新建 HttpClient**（无 keep-alive），海外站点每次重握手 1~3 秒；
- 不可达主机（arXiv/ERIC）没有熔断，同一批论文反复踩同一个坑；
- 前端轮询 24×5s=**2 分钟就停**，任务实际要跑几十分钟 → 用户看到"冻结"。

### 4. 一个好消息
命中率本身不低：任务 7 检查 57 位 → 30 个新可信邮箱（≈53%）；任务 8 检查 20 位 → +8（≈40%）。**瓶颈在"检查速度"和"假死"，不在匹配算法。**

---

## 二、本轮修复明细

| # | 文件（锚点） | 改动 |
|---|---|---|
| 1 | `AuthorDiscoveryProperties.java:39-50` | 新增 `extractionTimeoutSeconds=8`、`robotsTimeoutSeconds=5`、`hostFailureThreshold=3`、`maxPapersPerCandidate=4`、`maxUrlsPerPaper=4`；worker 默认 8→12 |
| 2 | `PublicEmailExtractor.java:60-116` | **共享 HttpClient**（keep-alive，连接超时≤5s）；**主机熔断**：同一主机连续失败 3 次后本进程内直接跳过（arXiv 这类站点不再反复白等）；抓取请求改用独立 8s 超时 |
| 3 | `RobotsTxtChecker.java:28-47` | 共享 HttpClient；robots 抓取独立 5s 短超时 |
| 4 | `AuthorDiscoveryService.java:155-172` | **停滞看门狗**：连续 10 次空轮询（≈10 分钟无任何候选完成）主动收尾，保留已入库结果并标记 PARTIAL——任务不再假死数小时 |
| 5 | `AuthorDiscoveryService.java:183` | 进度落库频率 %10→%5，前端进度更跟手 |
| 6 | `AuthorDiscoveryService.java:349,358` | 每候选论文 8→4、每论文 URL 8→4（已按 PDF 直链优先排序，砍的是长尾低产部分） |
| 7 | `application.yml:40-58` | 新参数显式写出；`max-pdf-bytes` 15MB→8MB（大 PDF 是卡死重灾区）；新增 `SEMANTIC_SCHOLAR_API_KEY` 环境变量入口 |
| 8 | `ConferenceUserManagementView.vue` | 轮询 2 分钟→**后台轮询直到任务终态**（上限 60 分钟）；按钮启动后即释放不再阻塞；页面加载时自动恢复 RUNNING 任务的进度展示；离开页面自动清理定时器 |

---

## 三、验证记录

| 项 | 结果 |
|---|---|
| 后端编译（intelliconf 模块） | ✅ BUILD SUCCESS，产物时间戳 13:52 已刷新 |
| 单测 | ✅ 18/18（AuthorDiscoveryCoreTest 13 + ConferenceMailServiceImplTest 5） |
| 前端 `npm run build`（含 vue-tsc） | ✅ 13.18s 成功 |

**验证插曲（重要）**：Maven 命令必须在 `intelliconf/` 目录下执行。仓库根目录 `D:\conference\newFront\pom.xml` 是一个**不同步的旧工程副本**——在根目录跑 Maven 会编译/测试旧代码且报 BUILD SUCCESS 假象（本轮一度误判 4/5 测试）。根目录下新生的 `target/` 是误产物，可删除；建议明确该旧工程的去留。

---

## 四、你需要做的

1. **重启后端服务** —— 会中断假死的任务 8（已抓到的 58 个邮箱全部保留，启动清理器会把它标记为已终止）；
2. **重新发起 200 人任务** —— 增量续跑自动跳过已有 58 个，目标补足剩余 142 个；按修复后的吞吐（预估 3~5 倍提速）和 ~50% 命中率，预计 30~60 分钟内完成；
3. 观察界面：进度现在会持续刷新直到终态，不会再"2 分钟后冻结"。

**可选增强**：到 Semantic Scholar 官网申请免费 API Key（1 分钟），设置环境变量 `SEMANTIC_SCHOLAR_API_KEY` 后第二数据源恢复可用，论文覆盖面进一步扩大。

---

## 五、遗留风险与后续建议

| 风险 | 说明 | 建议 |
|---|---|---|
| arXiv 不可达 | 网络环境限制（GFW 重置），该领域约 2~4 成 OA PDF 在 arXiv | 熔断已兜底不拖速度；如需利用，后续可给抓取链路加 HTTP 代理配置（需一个能路由 arXiv 的出口） |
| PDFBox 解析异常 PDF | 理论上仍可能卡住单个线程 | 看门狗保证任务整体 10 分钟内收尾，不再假死 |
| 根目录旧工程副本 | `newFront/pom.xml` 与 `intelliconf/` 代码不同步，易造成"构建成功"假象 | 建议删除或归档，避免误导后续构建 |
| 任务 7 提前收尾原因 | 疑似旧进程中断，无法完全复盘 | 新代码的看门狗+熔断已覆盖同类场景 |
