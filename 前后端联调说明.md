# IntelliConf 前后端联调配置说明

## 已完成的配置

### 1. 前端配置 (IntelliConfFront)

#### Vite 代理配置
在 `vite.config.ts` 中配置了开发服务器代理，将所有 `/api` 请求转发到后端：

```typescript
server: {
  port: 5173,
  proxy: {
    '/api': {
      target: 'http://localhost:8080',
      changeOrigin: true,
    }
  }
}
```

#### API 调用适配
修改了 `ExecutionDashboard.vue` 中的 API 调用：

1. **获取里程碑列表**
   - 前端调用：`/api/intelli-conf/v1/milestone/list?confereId={confId}`
   - 后端接口：`ConfMilestoneController.getMilestoneList()`

2. **获取任务日志**
   - 前端调用：`/api/task/logs/{milestoneId}`
   - 后端接口：`TaskLogController.getTaskLogs()`（新增）

3. **完成手动任务**
   - 前端调用：`/api/task/manual/complete/{logId}`
   - 后端接口：`ManualTaskController.completeTask()`

#### 数据字段适配
前端代码已适配后端的驼峰命名字段：
- `handlerBean` （后端） → 前端解析
- `executionStatus` （后端） → 前端映射为 status
- `taskName` （后端） → 前端使用
- `params` （JSON字符串） → 前端解析获取 hint 和 priority

### 2. 后端配置 (intelliconf)

#### 新增接口
创建了 `TaskLogController.java` 来提供任务日志查询功能：
- 路径：`/api/task/logs/{milestoneId}`
- 功能：查询指定里程碑下的所有任务日志（包含任务定义信息）
- 返回类型：`Result<List<TaskLogRespDTO>>`

#### 新增DTO
创建了 `TaskLogRespDTO.java` 来组合任务日志和任务定义的数据：
- 包含日志基本信息（id, executionStatus, errorMsg等）
- 包含任务定义信息（taskName, params, priority）

## 启动步骤

### 1. 启动后端服务

```powershell
cd D:\conference\newFront\intelliconf
.\mvnw spring-boot:run
```

或者在IDE中直接运行 `IntelliconfApplication.java`

后端将在 `http://localhost:8080` 启动

### 2. 启动前端服务

```powershell
cd D:\conference\newFront\IntelliConfFront
npm install  # 首次运行需要安装依赖
npm run dev
```

前端将在 `http://localhost:5173` 启动

### 3. 访问应用

打开浏览器访问：`http://localhost:5173`

## API 映射表

| 前端调用路径 | 后端实际路径 | Controller方法 | 说明 |
|-------------|-------------|---------------|------|
| `/api/intelli-conf/v1/milestone/list` | `/api/intelli-conf/v1/milestone/list` | `ConfMilestoneController.getMilestoneList()` | 获取里程碑列表 |
| `/api/task/logs/{milestoneId}` | `/api/task/logs/{milestoneId}` | `TaskLogController.getTaskLogs()` | 获取任务日志（新增） |
| `/api/task/manual/complete/{logId}` | `/api/task/manual/complete/{logId}` | `ManualTaskController.completeTask()` | 完成手动任务 |

## 数据字段映射

### 里程碑数据
- 后端返回：`{ allConfirmed: boolean, milestones: [...] }`
- 前端使用：`res.data.data.milestones`

### 任务日志数据
后端字段（驼峰命名） → 前端使用：
- `handlerBean` → 判断是否为手动任务（'manualTaskHandler'）
- `executionStatus` → 状态（0=待处理，1=执行中，2=成功，3=失败）
- `taskName` → 任务名称
- `params` → JSON字符串，需解析获取hint和priority
- `createTime` → 创建时间
- `errorMsg` → 错误信息

## 注意事项

1. **后端数据库配置**
   - 确保 `application.yml` 中的数据库连接信息正确
   - 数据库地址：`114.215.211.204:3306`
   - 数据库名：`intelliconf`

2. **CORS配置**
   - 由于使用了Vite代理，开发环境不需要额外配置CORS
   - 生产环境部署时需要在后端添加CORS配置

3. **端口占用**
   - 后端端口：8080
   - 前端端口：5173
   - 确保这些端口没有被其他程序占用

## 后续优化建议

1. **生产环境配置**
   - 配置后端CORS以支持跨域
   - 使用环境变量管理API基础URL

2. **错误处理**
   - 完善前端的错误提示
   - 添加请求超时处理

3. **性能优化**
   - 考虑添加请求缓存
   - 实现任务日志的分页查询

## 测试建议

1. 测试里程碑列表加载
2. 测试任务日志显示（包括手动任务和自动任务）
3. 测试完成手动任务功能
4. 测试错误处理（如网络异常、后端错误等）
