# 问题修复验证报告（2026-09-06）

本次处理三个问题：首页投稿者发现按钮跳转、潜在投稿者发现产出率低（200 目标仅 5 个）、邮箱绑定认证失败。

## 一、结论总览

| # | 问题 | 根因 | 处理结果 | 验证 |
|---|------|------|----------|------|
| 1 | 首页「投稿者发现」按钮跳转位置不对 | 当前源码已是直接跳转 `/author-discovery`，怀疑为旧构建/浏览器缓存 | 核实无误并重新构建前端 | 源码 + 新 dist 产物双重核验 ✅ |
| 2 | 投稿者发现 200 目标只抓到 5 个 | 单一论文源、4 线程顺序批处理、30 分钟时限、500 篇论文上限 | 功能升级（双论文源 + 并发收割 + 增量续跑） | 后端编译 + 13 个发现功能测试通过 ✅ |
| 3 | 邮箱绑定提示「邮箱认证失败，请检查账号和客户端专用密码」 | 未提交改动移除了密码 trim，复制的授权码带空格/换行导致 SMTP 认证失败 | 三层修复（绑定清洗 + 发送兜底 + 前端清洗） | 5 个邮件测试（含新增空格用例）通过 ✅ |

---

## 二、问题 1：首页投稿者发现按钮跳转

### 核查结果
首页（会议工作台 Dashboard）的「投稿者发现」卡片点击逻辑：

- `IntelliConfFront/src/views/DashboardView.vue:547`
  ```ts
  if (key === 'discovery') return router.push({ path: '/author-discovery', query: { confId: confId.value } })
  ```
- 路由 `IntelliConfFront/src/router/index.ts:17`：`/author-discovery` → `AuthorDiscoveryView.vue`（页面标题即「潜在投稿者发现」）。

**当前代码已经是"直接跳转到潜在投稿者发现页面"**，新构建产物（`dist/assets/index-Diycwkbh.js`）中也核验了相同逻辑。

### 建议
如果仍看到跳转异常，请 **Ctrl + F5 强制刷新**（或重启 `npm run dev`）排除旧缓存；本次已执行 `npm run build` 重新生成 dist。若强刷后仍复现，请提供点击后的地址栏 URL，可进一步定位。

---

## 三、问题 2：潜在投稿者发现功能升级

### 产出率低的四个瓶颈
1. 仅 OpenAlex 一个论文源，OA PDF 覆盖有限；
2. 邮箱核验只有 4 个线程，且按批次顺序等待（一个慢请求阻塞整批）；
3. 任务整体时限仅 30 分钟，目标 200 人根本跑不完；
4. 论文上限 500 篇，候选池太小；重复任务从零开始，浪费已抓成果。

### 升级内容（后端 `intelliconf`）

| 改动 | 位置 | 说明 |
|------|------|------|
| 新增 Semantic Scholar 论文源 | `author_discovery/service/SemanticScholarClient.java`（新文件） | S2 的 `openAccessPdf` 提供 PDF 直链，显著提高邮箱核验命中率；未配 API Key 按官方 100 次/5 分钟限速（3000ms 间隔 + 429 退避） |
| 双源合并检索 | `AuthorDiscoveryService.searchAllSources()` | OpenAlex + S2 按 DOI/标题去重合并，论文池上限 500→2000 |
| 并发模型重构 | `AuthorDiscoveryService.runJob()` | 4 线程顺序批处理 → 8 线程 `ExecutorCompletionService` 按完成顺序收割，消除慢请求阻塞 |
| 任务时限 | `AuthorDiscoveryProperties.jobDeadlineMinutes` | 30 分钟 → 180 分钟（配置项，可调） |
| 增量续跑 | `AuthorDiscoveryService.loadExistingEmails()` | 启动时预载该会议已入库邮箱，再次发起任务在既有成果上继续补充，自动跳过已有结果 |
| 双源同人合并 | `AuthorCandidateBuilder` | 有机构信息时按「姓名+机构」聚合，同一作者在双源的论文合并，核验机会翻倍 |
| PDF 直链优先 | `AuthorDiscoveryService.attachEmailEvidence()` | 优先抓取带 PDF 直链的论文（命中率最高），其次 OA 页 |
| 配置项扩展 | `AuthorDiscoveryProperties` | 新增 `semanticScholarEnabled / semanticScholarApiKey / jobDeadlineMinutes / emailExtractionWorkers / semanticScholarRequestIntervalMs`，均可在 `application.yml` 的 `author-discovery:` 下覆盖 |

### 前端配套改动
- `ConferenceUserManagementView.vue`：发现任务由同步 `/run`（600 秒超时）改为异步 `/start` + 每 5 秒轮询进度 2 分钟；超时后提示"任务仍在后台检索中"，与后端 180 分钟时限匹配，不会再因接口超时误报失败。

### 效果预期
- 论文池：~500 → 最多 2000 篇（双源去重后）；
- 核验吞吐：并发 8 + 完成序收割 + PDF 直链优先，单位时间核验人数约 2~3 倍；
- 时限：6 倍（30→180 分钟）；
- 目标 200 人可分多轮累积完成，PARTIAL 后再次发起即可续跑。

> 提示：若服务器能访问外网且需要更高 S2 配额，可申请免费的 Semantic Scholar API Key 并配置 `author-discovery.semantic-scholar-api-key`（限速自动放宽 3 倍）。

---

## 四、问题 3：邮箱绑定认证失败

### 根因
当晚未提交的改动在两处移除了密码 trim：
- `ConferenceMailServiceImpl.bindMailAccount`：`required(...)`（带 trim）→ `command.getPassword()`（原样）；
- `SmtpMailProvider.buildSender`：`setPassword` 不再 trim。

QQ/163/Gmail 的授权码从网页复制时几乎必然带首尾空格或换行（Gmail 应用专用密码更是分组显示 `xxxx xxxx xxxx xxxx`），原样提交后 SMTP 认证必然失败，报「邮箱认证失败，请检查账号和客户端专用密码」。

### 修复（三层防御）

| 层 | 位置 | 改动 |
|----|------|------|
| 绑定入口 | `ConferenceMailServiceImpl.java:76-81` | 授权码统一 `replaceAll("\\s+", "")` 剔除全部空白字符后再加密入库（各主流服务商授权码均不含合法空格） |
| 发送/测试兜底 | `SmtpMailProvider.java:80-82` | `setPassword` 前 trim + 去空白，兼容历史已入库的脏数据 |
| 前端提交 | `ConferenceMailAccountView.vue:204` | 提交前 `replace(/\s+/g, '')` |

另外把认证失败提示按服务商细分（`SmtpMailProvider.authFailedMessage()`）：163/126、QQ/腾讯企业邮、Gmail、Outlook 分别给出开启 SMTP、生成授权码的具体指引，替代原来的一句话泛化提示。

### 验证
新增单测 `bindStripsWhitespaceFromAppPassword`：输入 `"  abcd efgh ijkl mnop\n"`，断言入库解密后为 `abcdefghijklmnop`。✅ 通过

---

## 五、验证记录

| 项目 | 命令 | 结果 |
|------|------|------|
| 后端编译 | `mvn -DskipTests compile` | ✅ BUILD SUCCESS |
| 后端测试 | `ConferenceMailServiceImplTest`（5 个）+ `AuthorDiscoveryCoreTest`（13 个） | ✅ 18/18 通过 |
| 前端构建 | `npm run build`（含 vue-tsc 类型检查） | ✅ 构建成功，dist 已更新 |

## 六、下一步建议

1. **重启后端服务**后再测试邮箱绑定（代码改动需重启生效）；重新绑定时授权码可放心直接粘贴。
2. 重新发起一次 200 人发现任务验证产出率；建议从「首页 → 投稿者发现」进入，任务在后台运行，可离开页面稍后查看。
3. 首页按钮若仍跳错，先 Ctrl+F5 强刷；仍复现请提供地址栏 URL。
4. 可选：申请 Semantic Scholar API Key 提高检索配额；如需进一步提速，可在 `application.yml` 调大 `author-discovery.email-extraction-workers`。
